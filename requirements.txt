# Curated dependency list. The previous full pip-freeze is kept at
# requirements.txt.bak. Transitive deps (shiboken6, jupyter stack, etc.)
# are resolved automatically by pip.

# --- Runtime: GUI (ACFA_Schematic_Tool_GUI) ---
PySide6==6.9.0
requests==2.32.3

# --- Runtime: image tooling (util/thumbnails.py) ---
Pillow==11.2.1          # thumbnail decode (DXT1 -> image)
quicktex==0.3.1         # thumbnail encode (image -> DXT1/BC1); pure wheel, no system deps

# --- Notebook execution ---
ipykernel==6.29.5
jupyterlab==4.4.2

# --- Build (Windows .exe via GitHub Actions / pyinstaller) ---
pyinstaller==6.13.0
